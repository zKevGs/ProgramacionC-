#include <iostream>
#include <string>


using namespace std;

int main(){

/*

string Palabra = "Hola como estas";

    cout << "con length se ve asi: " << Palabra.length() << endl;
    cout << "con size se ve asi: " << Palabra.size() << endl;

/*

string userName ="";

    if(userName.empty()){
        cout << "ingrese su nombre de usuario nuevamente" << endl;
    }
    else{
        cout << "hola, " << userName;
    }

/*

    string userName ="";

cout << "esta vacio el nombre de usuario? " << (userName.empty() ? "si" : "no");

/*

string fraseIncompleta = "Hola";

    fraseIncompleta.append(" mundo");

cout << fraseIncompleta;

/*

string fraseIncompleta = "hola a todes quiero que agreguen un texto aca: ";

    fraseIncompleta.insert (fraseIncompleta.length(), "Puto");

cout << fraseIncompleta;

*/

return 0;
}
